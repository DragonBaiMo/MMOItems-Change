我将执行脚本生成代码变更补丁，以便开始审查。

# 变更摘要：修复 `/mi repair` 命令消息硬编码问题，增加死亡降级在线检查，并清理未使用的辅助类。

代码审查通过，未发现逻辑缺陷或安全风险。

以下是具体的变更确认（无需修复）：

- **`DurabilityUpdateHelper.java` (已删除)**
  - **注意**：该文件被完整删除。由于它包含 `@since 7.0` 的 Javadoc 且为 public 类，请确认没有其他插件或内部模块依赖此工具类。如果这是未发布的中间代码，则无风险。

- **`PlayerListener.java`**
  - **L286**：增加了 `!playerData.isOnline()` 检查。
  - **评价**：正确修复了玩家离线时可能导致的空指针异常或逻辑错误。

- **`RepairCommandTreeNode.java`**
  - **逻辑重构**：逻辑未变（主手/副手检查 -> 验证耐久 -> 修复 -> 更新背包），但改为使用 `Message` 枚举发送消息。
  - **评价**：实现正确，且利用 `DurabilityItem` 直接操作符合现有架构。

准备合并。