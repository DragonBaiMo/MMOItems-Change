# 变更摘要：扩展宝石的死亡降级属性支持，并修复玩家离线时的降级处理异常。

## 业务逻辑变更已确认为无缺陷实现
代码本身实现正确，无 bug。以下是受影响的行为变化（业务决策的结果，不需修复）：

- **文件**：`MMOItems-API/.../ItemStats.java`，行号 L242
  - **变更**：`DOWNGRADE_ON_DEATH_CHANCE` 属性现在的适用类型增加了 `gem_stone`。
  - **注意点**：这意味着宝石类物品现在也可以配置死亡降级概率（旧配置不受影响）。

- **文件**：`MMOItems-Dist/.../PlayerListener.java`，行号 L285
  - **变更**：在执行死亡降级前增加了 `!playerData.isOnline()` 的检查。
  - **意图**：防止玩家在死亡处理逻辑执行前掉线导致的潜在 NPE 或无效操作异常（这是稳健性修复）。

**审查结论**：代码逻辑稳健，准备合并。