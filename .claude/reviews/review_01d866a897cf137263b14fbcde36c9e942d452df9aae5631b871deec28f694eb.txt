我将首先检查 Git 仓库的状态，以确认有哪些文件发生了变更。

我将获取具体的代码变更差异，以便进行详细审查。

# 变更摘要：UpgradeStation GUI 重构，引入全局通用占位符支持

代码审查通过，未发现逻辑缺陷或安全风险。

## 业务逻辑变更已确认为无缺陷实现
代码本身实现正确，无 bug。以下是受影响的行为变化（业务决策的结果，不需修复）：

- **文件**：`UpgradeStationDisplay.java`
  - **变更**：新增 `buildPlaceholderContext` 与 `applyPlaceholders` 方法，将原本仅用于“进度标签”的占位符逻辑扩展至 GUI 内所有图标（包括按钮、状态图标等）。
  - **注意点**：`items.progress-label` 的 Lore 生成逻辑已更新。若配置文件中未定义 Lore，系统将使用默认格式；若定义了 Lore，则会应用新的通用占位符（如 `{rate}`, `{color}`, `{cost}` 等）。这保持了向后兼容性同时提供了更高的自定义自由度。

- **文件**：`upgrade-station.yml`
  - **变更**：更新了注释文档，列出了所有可用的占位符；更新了默认按钮配置以演示新占位符的用法。

审查通过。